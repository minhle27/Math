\begin{problem}{}{}{A Walk Through Combinatorics - Ch.9 Ex.5 p.225}
Let $G$ be a graph. Recall that we say that $H$ is an \emph{induced subgraph}
of $G$ if the vertex set of $H$ is a subset of that of $G$, and if $x$ and $y$ are
two vertices of $H$, then $xy$ is an edge in $H$ if and only if $xy$ is an edge
in $G$.

Let $G$ be a simple graph that has 10 vertices and 38 edges. Prove that
$G$ contains $K_4$ (the complete graph on four vertices) as an induced
subgraph.
\end{problem}

\begin{solution}

\end{solution}